# Dependencies for the Hugging Face Space (Gradio SDK), which only installs
# from a root-level requirements.txt. This is the demo's dependency subset of
# the main requirements.txt (no fastapi/uvicorn/httpx — those are API-only)
# plus gradio. When deploying, copy this file to requirements.txt at the root
# of the Space repo (see README.md for the exact steps).
langgraph==1.2.7
langchain==1.3.11
faiss-cpu==1.14.3
sentence-transformers==5.6.0
xgboost==3.3.0
scikit-learn==1.9.0
pandas==3.0.3
pydantic==2.13.4
gradio==6.20.0
